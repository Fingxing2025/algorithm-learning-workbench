# 智能算法学习助手 V2：仓库级开发约束

## 项目目标

本项目是一个本地优先、现代化的桌面算法学习工作台。它围绕“算法模板、题目、两者关联以及可配置 AI”展开，不是通用后台管理系统，也不是在线刷题平台。

所有实现必须遵循以下已确定范围：

- 保留并重做：模板树查询、模板入库、题目 AI 分析。
- 新增：题目卡片、模板与题目的多对多关联、总体文件 AI 管理、多供应商 AI 配置。
- 删除：学习诊断、算法 AI 讲解。
- 允许全量重构代码；V2 面向任何用户从零开始使用，不提供旧项目数据迁移或兼容导入。

旧项目位于相邻目录 `../智能算法学习助手`，仅作为功能行为参考。V2 不得依赖其目录、数据格式或个人模板；除非用户明确要求，不得修改旧项目。

## 开始任务前

1. 阅读 `docs/V2_PRODUCT_SPEC.md`、`docs/ARCHITECTURE.md`、`docs/IMPLEMENTATION_PLAN.md` 和 `docs/QUALITY_GATES.md`。
2. 检查 `git status`，已有改动属于用户，不得覆盖、回滚或顺带格式化。
3. 明确本次改动对应的产品需求和验收项；范围不清时先进行只读调查。
4. 涉及数据模型、IPC、文件移动、密钥存储或 AI 协议时，先写或更新架构决策记录。

## 固定技术方向

- 桌面：Electron。
- Renderer：React + TypeScript + Vite。
- UI：Tailwind CSS + shadcn/ui/Radix UI；图标使用 Lucide；动效使用 Motion。
- 本地数据：SQLite + Drizzle ORM；模板源码继续保留为用户可见文件。
- 校验：Zod。
- 测试：Vitest + React Testing Library + Playwright Electron E2E。
- 代码编辑器优先选 CodeMirror 6；只有明确需要 IDE 级能力时才引入 Monaco。

不得因为组件库已有默认样式就直接交付。必须建立项目自己的设计 token、主题和组件状态。

## 进程与安全边界

- Renderer 不得直接访问 `fs`、`path`、`child_process`、数据库或系统密钥。
- Renderer 不得启用 Node integration；必须启用 context isolation。
- Preload 只暴露命名清晰、参数经过校验的最小白名单 API，不暴露原始 IPC。
- Main process 负责文件系统、SQLite、系统对话框、密钥安全存储和网络请求。
- 所有文件路径都必须规范化并验证位于用户授权的模板根目录内。
- API Key 不得写入源码、日志、SQLite 明文字段、localStorage、截图或错误报告；使用操作系统安全存储，并在数据库中只保存引用。
- 外部链接使用系统浏览器打开，并验证协议和域名；禁止在应用窗口中加载任意远程页面。

## 数据保护

- 用户在 V2 中创建或授权访问的模板文件、题目卡片、题目图片和用户笔记都视为不可替代的用户数据。
- AI 只能生成文件整理计划；重命名、移动、合并、覆盖和删除必须先展示预览与 Diff，再由用户确认。
- 批量变更必须支持事务式失败回滚或操作前备份。
- 模板树的目录折叠首先是展示层行为，不得为了简化 UI 自动移动真实文件。
- 数据库 schema 变更必须提供 migration；禁止依赖删除数据库重新生成。
- 首次启动必须允许用户创建空白模板工作区或选择已有目录；选择已有目录时先只读扫描，不自动改名、移动或覆盖文件。
- 安装包、示例数据和默认配置不得包含开发者的个人目录、模板、题目、Provider 或密钥信息。

## AI Provider 规则

- 业务模块不得直接拼接供应商请求；统一经过 Provider Adapter 和任务路由。
- 首批协议边界：OpenAI-compatible Chat Completions、OpenAI Responses、Anthropic Messages、Gemini GenerateContent、Ollama。
- Provider 配置应描述 base URL、协议、模型、能力、超时和自定义请求头；密钥单独存储。
- 必须区分文本、视觉、结构化输出和流式能力；不支持的任务要在调用前阻止。
- 题目 AI 分析的结果先成为草稿，用户确认后才创建题目卡片和模板关联。
- 网络错误、鉴权错误、模型不存在、限流和不支持视觉必须给出不同的可操作提示。

## UI 与交互标准

- 默认界面语言为简体中文；技术名词保持一致。
- 核心布局采用桌面工作台结构：导航、列表/树、详情区；支持窗口缩放和面板尺寸记忆。
- 模板树支持单子目录折叠、搜索定位、键盘导航、虚拟滚动和右键菜单。
- 所有功能必须覆盖默认、悬停、焦点、选中、禁用、加载、空、失败和离线状态。
- 支持亮色/深色主题、可见焦点、合理对比度和减少动效偏好。
- 高频操作提供快捷键，至少包括全局搜索 `Cmd/Ctrl+K`。
- 不使用大段欢迎文案占据首屏；首页优先呈现最近模板、最近题目、快速搜索和待确认 AI 计划。
- 禁止通过堆叠渐变、玻璃效果和无意义动画代替信息层级。

## 工作方式

- 全量重构仍采用可验证的纵向切片：每个阶段交付可运行界面、数据流和测试。
- 不复制旧项目巨型 `app.js` 后再逐步修补；先抽取行为和数据契约，再实现新模块。
- 新依赖必须说明用途、维护状态、包体积或原生构建影响，避免功能重叠。
- 优先小而完整的提交；架构变更、视觉重构和数据库 schema 升级不要混在一个不可审查的改动里。
- 修改 UI 后使用 Playwright 和截图验证；涉及桌面权限与密钥时执行安全审查。

## 每次交付必须报告

- 完成了什么，以及对应的产品需求。
- 修改了哪些文件和数据结构。
- 执行了哪些检查、测试和截图验收。
- 全新安装、空白工作区和已有 V2 用户数据的兼容情况。
- 已知风险、未覆盖平台和下一步。

## 完成定义

只有同时满足以下条件，任务才算完成：

- 功能可以从真实桌面入口完成，而不只是组件单独渲染。
- 使用全新应用数据目录启动时，用户无需旧项目或预置个人数据即可完成首次设置并开始使用。
- 用户数据不会被静默修改或丢失。
- TypeScript、lint、单元测试和相关 E2E 通过。
- 核心状态已做亮色/深色和常用窗口尺寸截图检查。
- 错误信息对用户可操作，且日志不包含密钥或题目隐私内容。
