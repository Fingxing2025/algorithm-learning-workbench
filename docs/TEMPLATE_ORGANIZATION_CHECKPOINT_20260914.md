# 模板分类整理：本轮收尾与下次入口

2026-09-14，用户因剩余额度要求在合适阶段收尾。本轮停止继续扩展功能；以下是阶段交付，不是整个 S0–S5 规划全部完成。

## 唯一后续开发入口

根工作树和长期分支均为 `codex/template-organization-next`，继续开发从此分支开始。仓库 AGENTS.md 已记录该约定。不要返回旧的 8497/c43d/9e7e/d08c 工作树继续整合，也不要直接运行尚未接完的 S4 工作树去打开用户真实数据。

## 已合入

- S1 动态暂存：0009 增量迁移，逐项分类、取消/续作、重启恢复、预览后最终应用、目录交换与数据库事务、明确恢复入口、备份阻断。
- S3 分类：规范 taxonomy v2、同族合并、全局初步建议与详细源码判断分离、源码引用/覆盖核验、冲突送审、当前会话人工确认；修复 C++ 续行注释、数字分隔符和字符前缀证据边界。
- S2 评测：135 份 C++ 源码来自 27 个基础实现，按基础实现划分开发/留出集合，评分与扰动稳定性工具，分类目录与应用 taxonomy 精确契约。标签仍为 AI 草拟待人工校核。
- S1 独立审查修复 `d6cd47b`：规范物理工作区的独占运行时 SQLite 锁；不同 userData 也不能同时写同一工作区；崩溃由系统释放，失败切换保留旧数据库和所有权。
- S4 已完成的数据层 `05baa71`、路径修复 `becfda8`：确认仓储、逐项版本和哈希绑定、只读失效判定、0010 草案及定向测试。**0010 没有注册，持久化确认尚未接入产品入口，不能宣称跨重启确认已可用。**
- 开发依赖锁已修复审计问题；没有新增生产依赖或修改 Electron/SQLite 版本。

## 验证边界

- S1 独立切片：431 项 Vitest、9 项发布脚本、12 项 Electron；包含三个真实进程退出后的恢复。
- S1/S3 加所有权的工作树：60 文件 / 483 项 Vitest、TypeScript、lint、格式、9 项发布脚本通过；20 项相关 Electron 通过（暂存 5、入库 9、全库文件管理 5、空白备份及篡改 1），包含不同 userData/批次竞争、阻断主库写入、SIGKILL 后显式恢复、启动占用提示。首次仅一项旧分类路径断言失败，按规范路径修正后单独复跑通过，其余项已通过。这是相关场景集合，不冒充最终完整 E2E。
- 协调分支 `7c03514`：TypeScript 和 43 项分类证据/评测回归通过；所有权检查点合入前后 TypeScript、改动文件格式与 diff 检查通过。
- S4 数据层独立提交：完整基础检查通过，之后路径校验有独立回归；长期分支合入数据层后 TypeScript 与 4 文件 / 20 项确认数据层测试通过。完整桌面入口未接，未作相关 E2E。
- S1 的 483 项/20 项验证使用其独立旧工具锁（Vitest 4.1.10）；协调分支使用审计修复后的 4.1.11。两者不得混称最终同一环境的完整验证。详细证据见 `S1_S3_INTEGRATION_HANDOFF.md`，最终新锁全量验收留给 S5。
- 远端完整 CI 已通过的头是 `6a23e8e`（运行 `34804786627`），包括基础检查、Electron、macOS/Windows 打包。它早于本轮业务整合，**不代表本次最终头已完成整套验收**。本次推送触发的 CI 以实际结果为准。
- S5 完成了只读审查，并定位已回修的并发和证据问题；完整最终验收、最终安装包与最终版本截图验收未做。
- 真实 Provider 评测未运行；没有实际分类准确率，也没有达到 95%/98% 的结论。未调用个人 Provider、未将真实模板作为测试样本。

## 下次最小工作顺序

1. 先查看本分支最新 CI，再读 ADR-0040 与 S4 数据层；不要重做已完成的分类/暂存移植。
2. 完成 S4：注册 0010/schema，Main 从当前源码和规范规则重建确认快照；接命名 IPC/Preload、模板详情/单份/暂存确认释放；待复核项不能仅补元数据便应用。读取不写入，确认另一项不使前项失效，离线仍可手动整理。
3. 同步补全文件计划对有效确认的保护、源码/路径/分类规则变化后的失效、ID 重映射、删除撤销、备份显式表清单和跨工作区恢复。RC5 备份先验证原包，再只迁移临时副本，保留原包字节。
4. S5 对最终同一个 commit 做独立 npm ci、完整 check、fixture validate、Electron、亮暗三尺寸/关键缩放、新装与已有 V2 升级、备份往返和 packaged smoke；通过后才标记整轮实现完成。

## 保留现场

原始 89 份用户/旧工作树文件的备份位于仓库外 `20260914-111053-template-organization` 快照目录，原用户 `.codex/config.toml` 和三份未跟踪规划文档未修改。

额度收尾时另存仓库外 `20260914-quota-checkpoint`：S1/S4 二进制 diff、源码归档、HEAD/状态及逐文件 SHA-256 已核对。S1 源 HEAD 为 `0b0f060`，其 11 个业务/文档文件的检查点纳入 `d6cd47b`；原工作树没有被协调方改写。S4 保存时源 HEAD 为 `8d65034`，剩余四个接线文件随后在其独立分支提交为 `9635564`，工作树干净。**9635564 是未验收 WIP，注册了 0010 并开始更改请求契约，旧测试尚未同步；不得直接合入长期分支。**长期分支仅保留已验证的 S4 数据层。

这些快照均位于用户的 Documents/Codex/worktree-snapshots 下，不进入安装包。需要继续时以长期分支为准，将 S4 剩余差异逐项审查后使用，禁止直接覆盖最新代码。
