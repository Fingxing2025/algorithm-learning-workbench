# 模板分类与整理：执行计划及长期开发入口

日期：2026-09-14。长期分支：`codex/template-organization-next`。

用户已授权分配独立任务、实施分类整理规划、汇总成果，并要求以后在此分支继续开发。
本文件记录执行范围和验收证据，不能把已派发任务视为已完成实现。

## 目标与默认取舍

分类以源码证据为主，采用一个主分类与多个辅助标签/实现变体。优先降低错误的明确建议；未知、复合、证据不足或判断冲突的模板进入复核。导入与已有模板整理共用规范分类规则。

AI 只产生草稿。正式模板库变更必须经过 Diff、用户确认、版本复检和可恢复应用；元数据、题目关联、原始文件和其他工作区不得被静默损坏。用户已确认的分类应锁定，新的冲突只产生复核建议。

源码评测集中的预期标签若由 AI 草拟，明确标记待人工校核；Mock 结果、样本路径映射通过、软件绿测均不等于真实模型准确率。

## 基线与受保护来源

- 创建长期分支前已 `git fetch origin`，主线为 `0d9789f21875ee00a224c2a0d46502ad33ce52ef`。
- 根工作树已切换到长期分支，用户的 `.codex/config.toml` 和三份本地未跟踪规划文档保持原样。
- 根工作树和 `8497`、`c43d`、`9e7e`、`d08c` 的已修改/未跟踪文件已在仓库外保存归档、二进制 diff、Git 状态、HEAD 和逐文件 SHA-256。恢复材料不随安装包或源代码提交分发。
- `c43d` 是动态暂存来源；`8497` 是规范分类与全局批次语义来源；`9e7e` 是同义分类加固来源；`d08c` 只用于状态与错误处理查漏。
- 不重新移植已进入主线的模板树导出，不修改相邻旧项目，不使用真实用户模板作测试夹具。

## 任务分配

| 任务                    | 模型与推理                                | 范围                                                          | 依赖及交付                                    |
| ----------------------- | ----------------------------------------- | ------------------------------------------------------------- | --------------------------------------------- |
| S0 协调与整合           | 当前协调任务                              | 备份、长期分支、契约协调、提交审查、集成验收                  | 所有成果统一入口                              |
| S1 动态暂存与数据安全   | GPT-6 Astra / xhigh                       | 迁移 c43d，持久化、恢复、IPC、桌面入口、最终应用与回滚        | 可独立验收的暂存切片                          |
| S2 源码分类评测         | GPT-5.6 Sol → Terra / high                | 源码样本、分组隔离、结果契约、评分与区间、回归工具            | 与 S1/S3 独立；无实际 Provider 时标记 not-run |
| S3 分类证据与批次一致性 | GPT-6 Astra → GPT-5.6 Sol / xhigh         | 迁移 8497，修正串组、摘要覆盖、冲突复核、未知类别、同义归一化 | 独立提交后与 S1 合成统一状态机                |
| S4 统一复核与全库整理   | GPT-5.6 Sol / high，复杂冲突由 Astra 审查 | 在组合契约上完成证据、候选、状态与确认交互，用户纠正/锁定     | S1/S3 集成后启动，禁止提前覆盖相同文件        |
| S5 最终审查与验收       | GPT-6 Astra / high                        | 代码审查、完整检查、真实 Electron、截图、升级和打包烟雾       | S1–S4 完成后；发现问题返回负责切片修复        |

模型分配依据本机可用模型说明与任务的复杂度；不是宣称这些模型在本项目上已有比较实验。模型的一般适配参考 [OpenAI 模型文档](https://learn.chatgpt.com/docs/models)。

S2/S3 因模型容量错误切换到上述后备模型，保持原任务、代码现场及验收要求。

同一工作树只允许一个写入者。每个任务在自己的工作树提交，整合负责人按依赖审查并合入长期分支。不得共享可被 native rebuild 改写的 node_modules；Node 与 Electron 的 SQLite ABI 检查分开执行。

## 必须修正的分类风险

1. `reconcileBatchCategories` 的替换身份必须限定到模板/算法族，不能用跨组共享的泛化 categoryId 当全局键；冲突进入复核，不以自报最高分强行统一。
2. 全局摘要是初步提案，详细源码证据可以修订它；摘要/详细不一致、截断或证据不足必须显式呈现，不能把头尾源码当作完整实现。
3. 证据引用要在 Main 校验到源码位置；文件名、目录、注释不得直接决定类别。
4. 未知 ID 失败关闭，未知算法形成待确认类别提案；泛化分类不冒充精确分类。
5. 全局目录是上下文，不扩大当前批次可操作 ID；草稿与用户已确认事实具有不同可信状态。
6. 保存用户确认及来源版本，后续整理不得静默推翻；重新确认前不改正式分类或路径。

## 组合契约

- Main 拥有规范分类解析、路径计算、证据校验和复核判定；Renderer 不再单独制定批次归并结果。
- 暂存条目分别表达处理阶段、分类结论、复核状态和应用状态。跳过/失败/取消不伪装已分类。
- 会话保留工作区、源码哈希、分类规则/提示版本、全局事实版本和暂存版本。断点恢复校验旧事实是否仍有效；晚到响应不得覆盖新的人工编辑。
- 分类结果包括主 categoryId、候选、标签/变体、证据覆盖、复核原因和用户确认状态。具体契约以 S1/S3 集成后的 ADR 与 Zod 为准。
- 全库整理复用现有计划执行/备份体系；动态暂存导入复用 S1 恢复体系，不引入第二套互相竞争的正式数据写入入口。

## 提交与迁移安排

- 0034：规范分类；0035：保留主线核心元数据决策；0036：全局分类；0037：证据与复核；0038：动态暂存；0039：本轮整合与执行边界。
- 暂存沿用来源的 0009 migration；后续确认/锁定需要新的持久化结构时使用后续空闲编号，禁止覆盖已应用 migration。
- 切片依次包含决策/契约、实现及针对性测试；避免只有无法启动的散片。全量视觉重构、依赖升级、发行改版不混入。

## 验收门禁

软件：TypeScript、lint、格式、Vitest、release 脚本测试、受影响和最终完整 Electron E2E；全新 userData、空工作区、已有 V2 原位升级；关闭重启/取消/重试/失败/恢复；路径冲突、外部修改和版本变化；文件树、数据库、题目关联与操作日志一致。

界面：亮暗主题，1440×900、1280×720、1024×640 与关键 200% 缩放；证据、候选、冲突、待审、失败和确认按钮可达；截图需要人工复核。

质量：100 份以上含源码样本起步，明确基础实现数量与变体数量；按源码来源分组隔离开发/留出集合。记录主分类正确率、分族结果、低歧义 precision 与 coverage、未知/复合送审、错误硬分类和扰动稳定性。95% 总体和 98% 低歧义 precision 为待校准目标，必须带样本量/区间，未实测不能宣称达标。

发布：先本地测试包和 packaged smoke，不自动公开发布。macOS 本机证据不能替代 Windows 实机或正式签名/公证。

## 进度与证据

用户于本轮末因额度要求收尾，当前权威状态见 [本轮检查点与下次入口](TEMPLATE_ORGANIZATION_CHECKPOINT_20260914.md)。本计划保留全部目标，但未完成项不继续自动执行。

- S0：备份、长期分支、依赖审计修复和阶段整合完成；原用户文件保持原样。
- S1：`01a09de6-c427-7261-b934-c7fa612ea46d`。动态暂存、恢复、源码分类组合与跨实例所有权已纳入长期分支；483 项单元、20 项相关 Electron 通过。
- S2：`01a09de6-c427-7261-b934-c7d0cbfda7c1`。评测工具与 taxonomy 契约已纳入；135 源码/27 个基础实现，标签待人工校核，真实 Provider 未运行。
- S3：`01a09de6-c429-7a61-adee-069d7a71c9f3`。证据、冲突复核、规范分类及两次词法边界修复已纳入。
- S4：`01a09e08-0ef7-79b1-9f12-94f8cf1adfb5`。已验证数据层与路径修复纳入；0010 未注册，产品入口尚未接线。未完成接线 checkpoint `9635564` 留在独立分支，不直接合入。
- S5：`01a09e18-4caf-71e0-bb80-599c30d4a310`。只读审查发现已回修；完整最终验收本轮暂停。
- 远端完整 CI 通过的较早头为 `6a23e8e`，本次最终头的 CI 结果需单独查看；不能沿用旧头结果。

后续仅在长期分支继续 S4，再对最终同一提交执行 S5。无真实 AI 准确率结论，不公开发布安装包。
